/** Valores por defecto de tarifa domicilio — seguros para importar en el cliente (sin firebase-admin). */
export const DEFAULT_COSTO_DOMICILIO_COP = 4000;
export const DEFAULT_UMBRAL_GRATIS_COP = 35000;
